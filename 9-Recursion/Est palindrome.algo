PROCEDURE Test_Palindrom(ch : STRING, l;r:INTEGER)
BEGIN
IF (word.length=0 OR l>=r) THEN
write ("palindrom") ;
ELSE
IF (word[l]=word[r]) THEN
Test_Palindrom(word,l+1,r-1);
ELSE
write ("Not palindrom");
END_IF
END_IF
END
